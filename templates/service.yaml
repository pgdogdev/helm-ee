apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-control
  labels:
    app: {{ .Release.Name }}-control
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: {{ .Release.Name }}-control
