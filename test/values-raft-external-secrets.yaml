raft:
  enabled: true
  externalSecrets:
    enabled: true
    create: true
    secretStoreRef:
      name: aws-secrets-manager
      kind: SecretStore
    remoteRefs:
      - secretKey: token
        remoteRef:
          key: pgdog/raft
          property: token
